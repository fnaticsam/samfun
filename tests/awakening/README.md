Awakening mobile test: document metadata, responsive hooks, frozen source hashes, and generated output.
Run from the repository root: `node --test tests/awakening/*.test.mjs`.
It uses only Node's built-in test runner and Python 3 for the build reproduction check.
